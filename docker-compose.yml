version: '3.8'

services:
  edugenius-bot:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "14221:80"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: unless-stopped